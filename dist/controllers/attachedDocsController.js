"use strict";
// import OnlineDocs from "../plugins/online-docs";
// const idoc = new OnlineDocs();
// export const attachedDocument = async (attachFile: File): Promise<Buffer> => {
//     try {
//         // Assuming attachFile has a URL property to fetch the file
//         const fileUrl = attachFile.url;
//         // Create a PDF from the file URL
//         const pdfBuffer = await idoc.create(fileUrl);
//         // Return the PDF buffer
//         return pdfBuffer;
//     } catch (error) {
//         console.error('Error processing attached document:', error);
//         throw new Error(`Error processing attached document: ${error}`);
//     }
// };
